'use strict';
// describe('boilerplate page', function() {
  
//   beforeEach(function() {
//     browser.get('http://localhost/phonegap-apps/test-app/www/#/login');
//   });

//   it('should show with blank username and password', function() {
   
//     var username = element(by.model('user.username'));
//     var password = element(by.model('user.password'));

//     // expect(browser.getCurrentUrl()).toContain('login');
//     // expect(username.getAttribute('value')).toEqual('');
//     // expect(password.getAttribute('value')).toEqual('');
//   });
// });
